package com.liuboyu.jdk8.parallel;

import java.util.Map;
import java.util.Optional;
import java.util.stream.IntStream;

/**
 *
 * Created by Tony on 4/10/16.
 */
public class Examples {

    public Map<Integer, Double> parallelDiceRolls(int parallels) {
//        double fraction = 1.0 / parallels;
//        return IntStream.range(0, parallels).parallel().mapToObj();
        return (Map<Integer, Double>) Optional.empty().get();
    }

}
