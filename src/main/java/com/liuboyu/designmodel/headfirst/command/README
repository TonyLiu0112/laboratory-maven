# 命令模式
将"请求"封装成对象，以便使用不同的请求、队列或日志来参数化其他对象。命令模式也可以支持撤销操作

简单的例子可以想象一下餐厅：
食客、服务员、厨师

食客就是命令的发起者，服务员就是构建一个命令(菜单)，厨师就是命令的执行者。

代码例子：
1. 遥控器类，可以发起开灯、关灯命令；
2. 对于撤销命令的用法，实际上指的不是撤销当前命令，而是回滚命令到上一个状态(实现很简单，就是在具体的命令对象中，添加一个状态，每次执行命令
更新这个状态，需要回退的时候)